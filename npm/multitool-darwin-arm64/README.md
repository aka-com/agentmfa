# @aka-labs/multitool-darwin-arm64

The prebuilt Multitool `aka` CLI binary for macOS (Apple silicon). This package is
installed automatically as an optional dependency of
[`@aka-labs/multitool`](https://www.npmjs.com/package/@aka-labs/multitool) — install that
package instead of depending on this one directly.
