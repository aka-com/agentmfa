# agentmfa-darwin-arm64

The prebuilt AgentMFA (AKA) `aka` CLI binary for macOS (Apple silicon). This package is
installed automatically as an optional dependency of
[`agentmfa`](https://www.npmjs.com/package/agentmfa) — install that
package instead of depending on this one directly.
