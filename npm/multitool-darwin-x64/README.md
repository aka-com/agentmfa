# multitool-darwin-x64

The prebuilt Multitool `multitool` CLI binary for macOS (Intel). This package is
installed automatically as an optional dependency of
[`multitool`](https://www.npmjs.com/package/@aka-com/multitool) — install that
package instead of depending on this one directly.
