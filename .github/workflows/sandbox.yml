name: Docker sandbox

on:
  workflow_dispatch:
  schedule:
    - cron: '23 8 * * 2'

permissions:
  contents: read

jobs:
  sandbox:
    name: Real Postgres, SSH, HTTP, and MCP
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install test clients
        run: |
          sudo apt-get update
          sudo apt-get install --yes openssh-client postgresql-client

      - run: pnpm install --frozen-lockfile

      - name: Start sandbox
        run: pnpm run sandbox:up

      - name: Run sandbox suite
        run: pnpm run sandbox:test

      - name: Stop sandbox
        if: always()
        run: pnpm run sandbox:down
