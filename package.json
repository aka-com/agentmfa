{
  "name": "agentmfa",
  "private": true,
  "scripts": {
    "build": "bash scripts/build.sh",
    "start": "cargo run --manifest-path src-tauri/Cargo.toml --bin agentmfa-app",
    "release": "bash scripts/release.sh",
    "sync:icons": "node scripts/sync-lucide-icons.mjs",
    "test": "cargo test --workspace && cargo test --manifest-path src-tauri/Cargo.toml && node --test ui/tests/*.test.mjs",
    "clippy": "cargo clippy --workspace --all-targets && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "lucide": "^1.23.0"
  }
}
